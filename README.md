# rara-bdd

BDD testing framework for rararulab Rust projects. Parses Gherkin `.feature` files and evaluates acceptance criteria via a declarative YAML DSL (`.eval.yaml`).

## Quick Start

```bash
# Install
cargo install --git https://github.com/rararulab/rara-bdd

# Run BDD suite (JSON for CI)
rara-bdd run --features-dir features --report json

# List scenarios
rara-bdd list --features-dir features

# Validate eval files (schema check only)
rara-bdd validate --features-dir features

# Generate traceability matrix
rara-bdd trace --features-dir features
```

## Per-Project Setup

```
my-project/
├── features/
│   ├── auth.feature          # Gherkin scenarios with @AC-XX tags
│   ├── auth.eval.yaml        # YAML eval config (same stem as .feature)
│   └── TRACEABILITY.md       # Auto-generated by `rara-bdd trace`
├── src/
└── Cargo.toml
```

## Documentation

| Doc | Description |
|-----|-------------|
| [Architecture](docs/architecture.md) | Three-layer design, execution flow, data model |
| [YAML DSL Reference](docs/eval-dsl.md) | `runtime_tests`, `source_assertions`, `command_assertions` — all fields and examples |
| [CLI Reference](docs/cli.md) | Commands, flags, output shapes, exit codes |
| [Integration Guide](docs/integration.md) | Adding to existing projects, CI setup, best practices |

## Development

```bash
just fmt      # Format
just clippy   # Lint
just test     # Run tests
just lint     # Full lint suite
```

## License

MIT
